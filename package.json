{
  "name": "metaspace-avatars",
  "license": "UNLICENSED",
  "scripts": {
    "build": "graph build",
    "codegen": "graph codegen --debug --output-dir src/entities/",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ blockfishio/metaspace-rinkeby",
    "create-local": "graph create --node http://localhost:8020/ blockfishio/metaspace-avatars",
    "remove-local": "graph remove --node http://localhost:8020/ blockfishio/metaspace-avatars",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 blockfishio/metaspace-avatars"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
